[project]
name = "sfm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "selenium>=4.33.0",
]

[project.scripts]
sfm = "main:main"

[tool.uv]
package = true
